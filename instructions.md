Read infovac_ps.md, infovac_solution.md and progress_template.md.

Then read PROGRESS.md in full — the most recent entry is your context for what a
prior session (possibly a different model than you) already did. Treat its
"Decisions made and why" and "Do not reconsider" sections as settled — don't
re-derive them from scratch.

Resume at the "Exact next step" in the latest entry. Work toward the current
phase's Definition of Done only.

Critical: update PROGRESS.md continuously, not just at the end — append a new
entry (or extend the current one) after each meaningful sub-step, not only when
the phase fully completes. If you run out of context or get cut off mid-task,
the last thing in PROGRESS.md should be detailed enough that a model with zero
memory of this conversation can resume exactly where you stopped, including why
you made any non-obvious choice.

"Done" still means you ran the actual test and are reporting real output.